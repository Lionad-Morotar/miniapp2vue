<view class="page">
    <view class="header">
        <view s-if="checked">1</view>
        <view s-else>2</view>
    </view>

    <view class="red-body" data-id="testid" click="testclick">
        <view>{{msg}}</view>
    </view>
</view>
